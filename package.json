{
  "name": "ai-infra-studio",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"corepack pnpm -C apps/api dev\" \"corepack pnpm -C apps/web dev\"",
    "dev:e2e": "concurrently -n api,web -c green,blue \"corepack pnpm -C apps/api dev:e2e\" \"NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:4100 corepack pnpm -C apps/web dev:e2e\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "prettier . --check",
    "format:write": "prettier . --write",
    "test": "corepack pnpm -C apps/web test",
    "e2e": "corepack pnpm -C apps/web e2e"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "concurrently": "^9.1.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "typescript": "^5.7.2"
  }
}
